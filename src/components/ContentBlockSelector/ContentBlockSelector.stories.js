import React from 'react';
import { storiesOf } from '@storybook/react';
import { ContentBlockSelector } from './ContentBlockSelector';

storiesOf('<ContentBlockSelector />', module).add('ContentBlockSelector', () => (
  <div>
    <ContentBlockSelector />
  </div>
));
